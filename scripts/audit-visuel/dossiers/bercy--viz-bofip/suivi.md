# Suivi — Bulletins officiels des finances publiques

> Le journal de la transformation. Une ligne par intervention, la plus recente en haut.
> **Ce fichier n'est jamais ecrase par un script.**

| Date | Ce qui a change | Pourquoi | Verifie comment |
|---|---|---|---|
| 2026-09-26 | **R1** vocabulaire : « entrée en vigueur » → « version en vigueur » partout (h2, phrase de lecture, `databox-title`, `name`, description a11y, encadré) ; cartes « version du {{debut_de_validite:date}} ». **R2** phrase calculée sous le graphique (source `bofip-j0`, `count(*)` au 12/09/2012) + explication dans la description a11y. **R3** « commentaires et actualités » dans l'encadré et le sous-titre, avertissement sous les facettes, `{{#if division}}` dans le gabarit. **R4** phrase « année en cours » lue dans `max(debut_de_validite)` (source `bofip-max`). **R5** chiffres et date écrits à la main retirés de l'encadré et de #analyse. **R6** `group-by` en `date_format(…,'yyyy')` + `dsfr-data-normalize` dédié au tableau (`rename` + `round="cumul_part:1"`). **R7** voie contexte : `dsfr-data-context` visant `bofip` et `bofip-annees`, `context=` sur les facettes, sous-titre de méthode. **R8** section #analyse réécrite. **R9** `uid-field="permalien"`. **R10** compteur de la liste masqué visuellement (reste lu). | Relecture dataviz-metier (analyse-metier.md, R1-R10) | Playwright, dsfr-data 0.33.0, 0 erreur console, 0 HTTP ≥ 400. Chargement : 0 occurrence de « entrée/entrés en vigueur », « droit applicable », « corpus applicable », « 9 146 », « 3 septembre 2026 » hors #analyse ; carte 1 « BOI-TVA-DECLA-10-10-20 — version du 09/09/2026 » ; phrase « depuis 2018 ou avant … 52 % » ; « 1 354 documents … 12 septembre 2012 » ; « le 09/09/2026 » ; tableau « Année \| Documents \| Cumul \| Part cumulée (%) », « 2012 \| 1 478 \| 1 478 \| 16,2 » … « 2026 \| 611 \| 9 148 \| 100 ». Facette Actualité : 0/10 pastille vide, tableau « 2012 \| 67 \| 67 \| 2,4 », requête annuelle `where type = "Actualité"`. Recherche « investissements réalisés et exploités par les PME en Corse », page 2 : BOI-BIC-RICI-10-60-10 deux fois, 0 id DOM en double. Un seul « N documents » visible à chaque état (9 148 / 2 791 / 26 / 206). Survol barre 2012 : infobulle stylée « 2012 — 1478 documents ». `recette-pages.mjs` : err 0, kpi 1, graph 1. |
