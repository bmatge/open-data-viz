# Suivi — Tableau de bord Rappel Conso

> Le journal de la transformation. Une ligne par intervention, la plus recente en haut.
> **Ce fichier n'est jamais ecrase par un script.**

| Date | Ce qui a change | Pourquoi | Verifie comment |
|---|---|---|---|
| 2026-09-26 | Jeu basculé de `rappelconso-v2-gtin-trie` (1 ligne = 1 GTIN) à `rappelconso-v2-gtin-espaces` (1 ligne = 1 fiche) sur toutes les sources ; `rc-nature` (sans lecteur) retirée ; recherche en `server-search` + `search-template` restreint aux six champs annoncés (dont `libelle`) ; `order-by="date_publication:desc"` sur la source serveur ; courbe annuelle → barres par année close (`where … < now(month=1, day=1)`, année en texte par `date_format`) ; KPI « Année la plus chargée » et « 10 catégories » → « année en cours à ce jour » / « année précédente à la même date » (`now()`) ; catégories en part (`share_percent`, arrondi à 1) ; encadré « Ce qu'il faut retenir » daté ; phrase sur les 2 001 fiches sans nature ni compensation ; section #analyse réécrite (tableau de correspondance ajouté). | Relecture dataviz-metier R1-R6 : unité fausse (+48,5 %), un tiers des fiches absentes (véhicules), recherche limitée aux 12 lignes chargées, grille ouverte sur 2021, année en cours lue comme une baisse, axe des années « 2 021 ». | Playwright 2026-09-26 (dsfr-data 0.33.0 CDN) : KPI 18 699 / 2 520 / 2 192 = API (`records?limit=0`, fenêtres `now()`) ; recherche « 18 699 fiches de rappel » au chargement, « enamel paints » → 2 fiches dont 2025-03-0160 une fois, « ammo » → 17, « kool » → 4 (= API sur les six champs) ; requête `/records` avec `search(…)` et `order_by=date_publication DESC` ; 1re carte fiche 2026-09-0252 ; tableau a11y années 2021…2025 sans espace ; catégories 73,8 / 9,4 / 6,2 %… ; infobulle au survol « alimentation 73,8 % » et « 2025 3 148 » ; 0 erreur, 0 avertissement console ; `recette-pages.mjs` err 0, kpi 3, graph 2. |
