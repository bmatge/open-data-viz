# Suivi — Registre des aides de minimis

> Le journal de la transformation. Une ligne par intervention, la plus recente en haut.
> **Ce fichier n'est jamais ecrase par un script.**

| Date | Ce qui a change | Pourquoi | Verifie comment |
|---|---|---|---|
| 2026-09-26 | **R1** phrase « Sous quelle forme » réduite à ce que porte la donnée (queue « prêts, avances… se partagent le reste » retirée). **R2** bloc renommé « Qui verse le plus » (`data-code` « …par montant versé »), « Une seule autorité » retiré, seconde phrase calculée (classement en nombre : `mi-par-autorite-nb` + `limit="1"` + `dsfr-data-repeat`), nombre d'aides sous chaque barre du podium (`subtitle-field` nourri par un `dsfr-data-normalize compute` qui pose le séparateur de milliers). **R3** effectif « 16 610 » retiré de l'encadré, `max-records` 20 000 → 100 000, commentaire daté. **R4** KPI « Dernière aide octroyée » (`date_octroi:max`, `format="date"`), période « octroyées depuis le 1er janvier 2026 » dans le libellé du KPI « Au registre » et dans l'encadré, avec le délai de déclaration. **R5** KPI agricole : littéral `value="=Non publié"`, query `mi-agri` supprimée. **R6** phrase vivante sous le tableau (group-by sur six champs + agrégat global + `{{#if}}`), et mention dans l'encadré. **R7** camembert → barres horizontales, pleine largeur, `name` de série. **R9** date du tableau en JJ/MM/AAAA (`date_format` dans le `select`), colonne « Secteur (code NAF) ». **R8** chiffres de cardinalité réécrits dans #analyse. Hors relecture : KPI de régime passés à deux par ligne (« 174 536 650 € » débordait sa tuile : 251 px pour 208). Section #analyse réécrite. | Relecture `dataviz-metier` (analyse-metier.md, R1 à R9) | `scripts/recette-pages.mjs` : err 0, kpi 6, graph 1. Playwright (0.33.0, 0 erreur console, 4 avertissements #765 distincts dont 2 préexistants) : sans filtre, podium DGDDI 27 338 842 € / 5 164 aides, DGE 22 329 568 € / 6 872 ; phrases « DGDDI … 14,4 % … 5 164 aides » et « En nombre d'aides, c'est DGE … (6 872) » ; phrase doublons « 17 621 lignes, 17 400 distinctes, 221 autres ». Instrument = Prêt/Avances → « concentre 100,0 % des montants de la sélection. » (rien d'autre). Autorité = DGE → deux phrases DGE, 6 872, podium 22 329 568 €, instrument 56,7 % (tableau a11y : Prêt/Avances 56,7 + Subvention directe 43,3). Régime SIEG → 98,3 % subvention directe, a11y Subvention directe + Avances récupérables seulement ; KPI général et pêche 0 €, agricole « Non publié » inchangé ; « Aucune répétition exacte ». Survol du graphique : infobulle « Subvention directe 169 305 360,34 € », fond blanc, `position: absolute`. KPI : aucun `scrollWidth` > largeur de tuile. API le même jour : `total_count` 17 621. |
